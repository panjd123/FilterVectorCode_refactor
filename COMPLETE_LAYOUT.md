# Complete Layout Note

This repository now includes baseline runtime paths copied from `GPU/FilterVectorCode`:

- `UNG/codes/**`
- `ACORN/**`
- top-level scripts: `build_hybrid.sh`, `exp.sh`, `generate_gt.sh`, `generate_queries.sh`, `search.sh`
- top-level config: `experiments.json`

This provides a complete baseline tree for build/run and subsequent optimization replay.
